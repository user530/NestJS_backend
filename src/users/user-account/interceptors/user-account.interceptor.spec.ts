import { UserAccountInterceptor } from './user-account.interceptor';

describe('UserAccountInterceptor', () => {
  it('should be defined', () => {
    expect(new UserAccountInterceptor()).toBeDefined();
  });
});
